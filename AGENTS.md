This is a base repository to build new ideas from.

If the code is written in rust:
* When inputting genomes as fasta files, prefer to use add_genome_specification_arguments from the bird_tool_utils crate
* Add tests
* Document each method briefly
